# Creamos un conjunto llamado 'planetas' con cuatro elementos.
planetas = {"Mercurio", "Venus", "Tierra", "Marte"}

# Usamos la función 'len()' para obtener el número de elementos en el conjunto.
# La función 'len()' devuelve 4, porque hay cuatro planetas.
print(len(planetas))